export const ADDON_ID = "codpenLauncher";
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = "codpenLauncher";

export const EVENTS = {
    RESULT: `${ADDON_ID}/result`,
    REQUEST: `${ADDON_ID}/request`,
};
